# 变更日志



## [1.1.0] - 2026-08-17

### 新增

- 任务标签：可选一级标签（学习 / 工作 / 健身 / 休息 / 其他），选中后可填二级自定义标签（如「数学」）。
- 统计面板新增「分类统计」：按「一级·二级」标签聚合当前筛选范围内的有效时长。
- 历史记录支持「再来一次」：一键用相同时长与标签回到倒计时页重新开始。
- 进行中任务持久化：计时状态实时落库，App 被系统杀掉或重启后自动恢复未完成任务；已过期的自动补记为正常完成。

### 变更

- 数据库版本升级 v1 → v2：`timer_records` 新增 `category`、`sub_label` 列，新增 `active_task` 表（含旧库迁移，历史数据不丢失）。

## [1.0.0] - 2026-08-14

### 新增

- 倒计时任务：时 / 分 / 秒输入，大号数字实时显示剩余时间。
- 暂停 / 继续（每个任务最多 3 次，用完后「暂停」置灰）、停止弹窗三选项（放弃 / 提前完成 / 取消）、重置。
- 有效时长规则：扣除暂停时间；开始后 30 秒以内终止不计入统计。
- 倒计时结束触发震动提醒并自动保存有效时长。
- 数据统计：今日 / 本周 / 本月 / 自定义时间范围四种筛选，展示总有效时长与记录列表。
- 基于系统时间戳差值计时（防漂移）、SQLite 本地持久化、深色模式、仅振动权限。
